import 'package:flutter/material.dart';

class TestFiller2 extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          "ANALYTIC",
          style: TextStyle(
            fontSize: 50.0
          )
        )
      )
    );
  }
}