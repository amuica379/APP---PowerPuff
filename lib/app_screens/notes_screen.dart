import 'package:flutter/material.dart';

class TestFiller4 extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          "NOTAS",
          style: TextStyle(
            fontSize: 50.0
          )
        )
      )
    );
  }
}